export const CONFIG = {
  // For dev on your local network later you can swap this.
  // For now, point to your Render URL (or whatever is stable).
  BASE_URL: "https://call-intercept.onrender.com",
};
